<%= simple_form_for @product do |f| %>
  <%= f.input :name %>
  <%= f.input :weight %>
  <%= f.input :unit_cost %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <%= f.button :submit %>
<% end %>
